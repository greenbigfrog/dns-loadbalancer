name: dns-loadbalancer
version: 0.1.0

authors:
  - Jonathan B. <greenbigfrog@gmail.com>

targets:
  dns-loadbalancer:
    main: src/dns-loadbalancer.cr

dependencies:
  cryflare:
    github: GrottoPress/cryflare
  dotenv:
    github: drum445/dotenv

crystal: 0.36.1

license: MIT
